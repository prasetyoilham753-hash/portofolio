# Environment Variables
# Disable Vite HMR when true (configured by AI Studio)
DISABLE_HMR=true

# Authorized Administrator Email (Optional client fallback, primary authorization is Firestore admins/{uid})
VITE_ADMIN_EMAIL=
